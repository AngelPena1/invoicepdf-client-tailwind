const { filterString } = require("./filterString");

test("Given a string, search for similars in array", () => {
  const array = ["Angel, Angela, Angelo, Eduardo, Eduarda"]
  const string = "Ang"
});
