// import React from "react";

// const Index = ({ id, classname, onClick, onChange, value }) => {
//   return <input id={id} className={classname} onClick={onClick} onChange={} />;
// };

// export default Index;
